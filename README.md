# Microclimate — Локальная версия (SQLite)

Система мониторинга микроклимата для локального использования.

## 📋 Требования
- Python 3.8+ (рекомендуется 3.11)

## 🚀 Быстрый старт

### 1. Установка зависимостей

**macOS/Linux:**
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

**Windows:**
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Запуск сервера

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

При первом запуске автоматически создастся:
- ✅ База данных `sql_app.db`
- ✅ Все необходимые таблицы
- ✅ Папка `reports/` для отчётов

**Swagger UI доступен:** http://127.0.0.1:8000/docs

### 3. Наполнение тестовыми данными

Через Swagger UI или curl:
```bash
curl -X POST http://127.0.0.1:8000/api/seed_data
```

Это создаст:
- 3 кабинета с датчиками
- Температурные и влажностные сенсоры
- 24 часа исторических данных для каждого датчика
- Тестового пользователя

### 4. Запуск симулятора датчиков

Откройте `simulator.py` и убедитесь, что URL правильный:
```python
BASE_URL = "http://127.0.0.1:8000"
```

Запустите симулятор:
```bash
python simulator.py
```

Симулятор будет каждые 30 секунд отправлять данные с датчиков.

## 📁 Структура проекта

```
microclimate/
├── main.py              # FastAPI приложение
├── database.py          # SQLite подключение
├── models.py            # SQLAlchemy модели
├── schemas.py           # Pydantic схемы
├── crud.py              # Бизнес-логика
├── simulator.py         # Симулятор IoT датчиков
├── requirements.txt     # Зависимости
├── sql_app.db          # База данных SQLite (создаётся автоматически)
└── reports/            # Папка с отчётами (создаётся автоматически)
```

## 🔑 Основные эндпоинты

### Датчики
- `GET /api/locations` - Список всех кабинетов
- `GET /api/sensors/{location_id}` - Датчики в кабинете
- `PATCH /api/sensors/{sensor_id}` - Управление датчиком

### Аналитика
- `GET /api/dashboard/stats` - Статистика дашборда
- `GET /analytics/{sensor_id}` - Данные для графиков
- `GET /api/history` - История измерений

### Отчёты
- `GET /api/reports` - Список отчётов
- `POST /api/reports/generate/week` - Сгенерировать недельный отчёт
- `POST /api/reports/generate/month` - Сгенерировать месячный отчёт
- `GET /api/reports/{report_id}/download` - Скачать отчёт

### IoT
- `POST /api/measurements` - Принять данные с датчика

## 🎯 Основные возможности

### ✨ Автоматическая физика датчиков
При изменении целевого значения (слайдер), датчик **плавно** подстраивается к новому значению, имитируя реальное поведение кондиционера/увлажнителя.

### 📊 Отчёты
Отчёты сохраняются в папку `reports/` и доступны для скачивания через API.

### 📝 Логирование действий
Все изменения настроек датчиков записываются в историю действий с указанием пользователя.

## 🛠 Настройки (опционально)

### Изменить путь к базе данных
```bash
export DATABASE_FILE="./my_custom.db"
uvicorn main:app --reload
```

### Изменить интервал симулятора
В файле `simulator.py`:
```python
INTERVAL_SECONDS = 10  # каждые 10 секунд
```

## 📦 Бэкап базы данных

Просто скопируйте файл:
```bash
cp sql_app.db sql_app.db.backup
```

## 🧹 Очистка данных

Удалите файл базы данных:
```bash
rm sql_app.db
```

При следующем запуске создастся новая пустая база.

## ❓ Проблемы и решения

**База данных не создаётся:**
- Проверьте права на запись в текущей директории
- Убедитесь, что установлен SQLAlchemy

**Симулятор не отправляет данные:**
- Проверьте, что сервер запущен на `http://127.0.0.1:8000`
- Убедитесь, что выполнили `/api/seed_data` для создания датчиков

**Отчёты не сохраняются:**
- Проверьте, что папка `reports/` существует и доступна для записи

## 📝 Лицензия

Дипломный проект